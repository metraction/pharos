package mq

import (
	"testing"
)

func TestTaskQueueDsn(t *testing.T) {

	// streamName, groupName, maxLen, maxRetry, maxTTL, err := mq.ParseTaskQueueDsn("queue://mx:scantask/?maxlen=1000&maxretry=2&maxttl=1h")

	// assert.NoError(t, err)
	// assert.Equal(t, "mx", streamName)
	// assert.Equal(t, "scantask", groupName)
	// assert.Equal(t, int64(1000), maxLen)
	// assert.Equal(t, int64(2), maxRetry)
	// assert.Equal(t, 1*time.Hour, maxTTL)

}
